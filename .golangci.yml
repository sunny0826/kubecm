version: "2"
run:
  timeout: 10m
  tests: true
linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - stylecheck
    - tagliatelle
  disable:
    - unused
    - deadcode
  exclusions:
    paths:
      - vendor$
      - third_party$
      - hack